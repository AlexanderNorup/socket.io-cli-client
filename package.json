{
  "name": "socket.io-cli-client",
  "version": "1.0.0",
  "description": "Allows for connecting to a socket.io server and recieve/send messages",
  "main": "client.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexander Nørup",
  "license": "ISC",
  "dependencies": {
    "ansi-color": "^0.2.1",
    "socket.io-client": "^2.3.0"
  }
}
